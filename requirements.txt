PySide6>=6.6
# Contrib build required: ChArUco (cv2.aruco) lives in the contrib modules.
# Never install opencv-python alongside it - the two packages overwrite each
# other's cv2 module and a build could silently lose aruco support.
opencv-contrib-python>=4.8
numpy>=1.26
scipy>=1.11
# Auto-update framework (installer + self-updating from GitHub Releases).
velopack>=1.2

"""Multi-camera calibration desktop application."""

# Single source of truth for the application version. The build script
# (build_release.ps1) reads this to set the Velopack package version, and the
# in-app updater shows it to the user. Bump this before every release.
__version__ = "1.0.10"
